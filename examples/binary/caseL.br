fix id(z). Lam. Lam. lam l. caseL l of 
   nil =>  nil
 | h::tl => h :: tl,

fix id(z). Lam. Lam. lam l. caseL l of 
   nil =>  nil
 | h::tl => h :: tl

<= 0 : unitR => forall i; alpha. (list[i,alpha] U bool) => list[i,alpha] U bool
